{
  "table": "users",
  "columns": [
    {"name": "id", "type": "INT"},
    {"name": "username", "type": "VARCHAR"},
    {"name": "email", "type": "VARCHAR"},
    {"name": "age", "type": "INT"}
  ],
  "rows": [
    [1, "alice", "alice@example.com", 30]
  ],
  "instructions": "When clients query the users table, respond with mysql_query_response action using these exact columns and rows."
}
