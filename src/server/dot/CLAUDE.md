# DNS-over-TLS (DoT) Protocol Implementation

## Overview

DNS-over-TLS server implementing RFC 7858 for secure DNS queries over TLS transport. The LLM controls DNS responses
while NetGet handles TLS encryption and connection management.

**Status**: Beta (Core Protocol)
**RFC**: RFC 7858 (DNS over TLS), RFC 1035 (DNS), RFC 5246 (TLS 1.2)
**Port**: 853 (TCP with TLS)

## Library Choices

- **hickory-proto** (formerly trust-dns) - DNS protocol parsing and construction
    - Reuses DNS wire format handling from standard DNS implementation
    - Parses queries and constructs responses
    - Handles all DNS record types (A, AAAA, MX, TXT, CNAME, etc.)
- **tokio-rustls** - Async TLS server implementation
    - Provides TLS acceptor for TCP streams
    - Uses rustls for TLS protocol
    - Self-signed certificates generated automatically
- **rustls** - Modern TLS library
    - Pure Rust implementation
    - Supports TLS 1.2 and 1.3
    - Memory-safe and performant

**Rationale**: DoT is essentially DNS delivered over TLS instead of plain UDP/TCP. By combining hickory-proto's DNS
handling with tokio-rustls's TLS transport, we get a complete secure DNS solution. The LLM can focus on DNS semantics
while the libraries handle encryption.

## Architecture Decisions

### 1. Delegation to DNS Protocol

DoT delegates all DNS logic to the standard DNS protocol implementation:

- `DotProtocol` wraps `DnsProtocol`
- All action definitions come from DNS actions
- Action execution forwards to DNS implementation
- This ensures consistency between DNS and DoT responses

### 2. TLS Transport Layer

Connection flow:

1. Accept TCP connection on port 853
2. Perform TLS handshake using self-signed certificate
3. Read length-prefixed DNS messages (2-byte big-endian length + DNS packet)
4. Parse DNS query with hickory-proto
5. Send to LLM for response
6. Write length-prefixed DNS response back over TLS
7. Loop for multiple queries per connection

### 3. Length-Prefixed Messages

DoT uses TCP framing (unlike standard DNS UDP):

- Each message prefixed with 2-byte big-endian length
- Allows multiple queries per connection
- Prevents message fragmentation issues
- Read: `read_exact(&mut [0u8; 2])` then `read_exact(&mut vec![0u8; len])`
- Write: `write_all(&len.to_be_bytes())` then `write_all(&dns_bytes)`

### 4. Self-Signed Certificates

TLS configuration:

- Certificates generated by `tls_cert_manager::generate_default_tls_config()`
- Self-signed for testing/development
- Clients must disable certificate verification for testing
- Production usage would require proper CA-signed certificates

### 5. Dual Logging

- **DEBUG**: Query summary ("DoT query: example.com A"), TLS handshake events, byte counts
- **TRACE**: Full hex dumps of DNS packets (both request and response)
- Both go to netget.log and TUI Status panel

### 6. Connection Persistence

Unlike UDP DNS:

- Connections remain open for multiple queries
- Same TLS session reused for efficiency
- Client can pipeline queries
- Connection closed when client closes or read fails

## LLM Integration

### Event Type

**`dot_query`** - Triggered when DNS query received over TLS

Event parameters:

- `query_id` (number) - DNS transaction ID from request packet
- `domain` (string) - Domain name being queried
- `query_type` (string) - Record type (A, AAAA, MX, TXT, etc.)
- `peer_addr` (string) - Client IP address and port

### Available Actions

DoT reuses all DNS actions:

- `dns_response` - Send DNS response as hex-encoded packet
- `send_dns_a_response` - Return IPv4 address (A record)
- `send_dns_aaaa_response` - Return IPv6 address (AAAA record)
- `send_dns_mx_response` - Return mail exchange record
- `send_dns_txt_response` - Return text record
- `send_dns_cname_response` - Return canonical name alias
- `send_dns_nxdomain` - Domain does not exist
- `ignore_query` - Don't send response
- `close_connection` - Close TLS connection

See `src/server/dns/CLAUDE.md` for detailed action documentation.

### Example LLM Response

```json
{
  "actions": [
    {
      "type": "send_dns_a_response",
      "query_id": 12345,
      "domain": "example.com",
      "ip": "93.184.216.34",
      "ttl": 300
    },
    {
      "type": "show_message",
      "message": "Resolved example.com to 93.184.216.34 over TLS"
    }
  ]
}
```

## Connection Management

### Connection Lifecycle

1. **Accept**: TCP listener accepts connection on port 853
2. **TLS Handshake**: `TlsAcceptor::accept()` performs TLS negotiation
3. **Register**: Connection logged in INFO messages
4. **Query Loop**: Read DNS queries, call LLM, send responses
5. **Close**: Connection ends on client disconnect or error

### State Management

- DoT connections are stateful (persistent TCP+TLS)
- Multiple queries share same TLS session
- No connection state stored in ServerInstance (simple protocol)
- Each query is independent at DNS layer

## Known Limitations

### 1. Self-Signed Certificates Only

- No support for custom certificates
- No CA-signed certificate loading
- No certificate rotation/renewal
- Testing requires disabling certificate verification on clients

**Workaround**: For production, modify `tls_cert_manager` to load custom certificates.

### 2. No Client Authentication

- Server doesn't request client certificates
- No mutual TLS (mTLS) support
- All clients accepted after TLS handshake

### 3. DNS Limitations Inherited

All limitations from standard DNS protocol apply:

- Single answer per action (use `dns_response` for multiple)
- Limited record type support
- No DNSSEC
- No recursive resolution
- See `src/server/dns/CLAUDE.md` for full list

### 4. No Connection Pooling

- Each connection handled independently
- No connection limits or rate limiting
- Memory usage grows with concurrent connections

### 5. No EDNS0 Support

- No support for Extended DNS (RFC 6891)
- No client subnet information
- No large UDP response fallback (DoT always uses TCP)

## Example Prompts

### Basic DoT Server

```
listen on port 853 via dns-over-tls
Respond to all A record queries for example.com with IP 93.184.216.34
For all other domains, return NXDOMAIN
```

### Multi-Record DoT Server

```
listen on port 853 via dot
For secure.example.com:
  - A record: 93.184.216.34
  - AAAA record: 2001:db8::1
  - MX record: mail.secure.example.com with priority 10
For all unknown domains, return NXDOMAIN
```

### Privacy-Focused DNS

```
listen on port 853 via dns-over-tls
Provide DNS resolution with TLS encryption for privacy
For example.com: return 93.184.216.34
For mail.example.com: return 93.184.216.35
Block tracking domains by returning NXDOMAIN
```

## Performance Characteristics

### Latency

- **TLS Handshake**: ~50-100ms (one-time per connection)
- **Per Query (with scripting)**: Sub-millisecond (after handshake)
- **Per Query (without scripting)**: 2-5 seconds (LLM call)
- hickory-proto parsing: ~10-50 microseconds
- Length-prefix overhead: ~1 microsecond

### Throughput

- **With Scripting**: Thousands of queries per second per connection
- **Without Scripting**: ~0.2-0.5 QPS per connection (LLM-limited)
- Connection reuse amortizes TLS handshake cost
- Multiple concurrent connections supported

### Comparison to UDP DNS

- **Advantage**: Encrypted, tamper-proof, NAT-friendly
- **Disadvantage**: Higher latency (TLS handshake, TCP overhead)
- **Use Case**: Privacy-sensitive DNS queries, corporate environments

### Scripting Compatibility

Excellent scripting candidate:

- Same query/response pattern as DNS
- Deterministic responses based on domain
- High query volume typical
- Connection reuse makes scripting very efficient

## Security Considerations

### TLS Protection

- Prevents eavesdropping on DNS queries
- Prevents DNS spoofing/tampering
- Ensures query integrity
- Authenticates server (with proper certificates)

### Self-Signed Certificate Risks

- Default setup vulnerable to MITM attacks
- Clients must explicitly trust self-signed cert
- Not suitable for production without proper PKI

### Privacy Benefits

- ISP/network operators cannot see DNS queries
- Prevents DNS-based tracking
- Protects against DNS hijacking
- Complements HTTPS for end-to-end privacy

## References

- [RFC 7858: Specification for DNS over Transport Layer Security (TLS)](https://datatracker.ietf.org/doc/html/rfc7858)
- [RFC 8310: Usage Profiles for DNS over TLS and DNS over DTLS](https://datatracker.ietf.org/doc/html/rfc8310)
- [RFC 1035: Domain Names - Implementation](https://datatracker.ietf.org/doc/html/rfc1035)
- [hickory-proto Documentation](https://docs.rs/hickory-proto/latest/hickory_proto/)
- [tokio-rustls Documentation](https://docs.rs/tokio-rustls/latest/tokio_rustls/)
- [rustls Documentation](https://docs.rs/rustls/latest/rustls/)
